---
layout: layout.liquid
title: Blog
---

<h2>blogs here: </h2>
<div>
    {% for post in collections.post %}
        <article>
            <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
            <p>{{ post.templateContent | truncate: 100 }}</p>
        </article>
    {% endfor %}
</div>
<a href="/">Home</a>